{
  "name": "neo4j-cpg-edges-sink",
  "config": {
    "connector.class": "org.neo4j.connectors.kafka.sink.Neo4jConnector",
    "topics": "cpg.edges",
    "key.converter": "org.apache.kafka.connect.storage.StringConverter",
    "value.converter": "org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable": "false",
    "neo4j.uri": "bolt://neo4j:7687",
    "neo4j.database": "neo4j",
    "neo4j.authentication.basic.username": "neo4j",
    "neo4j.authentication.basic.password": "cpgpassword",
    "neo4j.cypher.topic.cpg.edges": "MERGE (a:CPGNode {id: event.source_id}) MERGE (b:CPGNode {id: event.target_id}) FOREACH (_ IN CASE WHEN event.edge_type = 'CFG' THEN [1] ELSE [] END | MERGE (a)-[r:CFG]->(b) SET r.edge_id = event.edge_id, r.file_path = event.file_path, r.event_time = event.event_time) FOREACH (_ IN CASE WHEN event.edge_type = 'DFG' THEN [1] ELSE [] END | MERGE (a)-[r:DFG]->(b) SET r.edge_id = event.edge_id, r.file_path = event.file_path, r.event_time = event.event_time) FOREACH (_ IN CASE WHEN event.edge_type = 'CALL' THEN [1] ELSE [] END | MERGE (a)-[r:CALL]->(b) SET r.edge_id = event.edge_id, r.file_path = event.file_path, r.event_time = event.event_time) FOREACH (_ IN CASE WHEN event.edge_type = 'CALL_RESOLVES_TO' THEN [1] ELSE [] END | MERGE (a)-[r:CALL_RESOLVES_TO]->(b) SET r.edge_id = event.edge_id, r.file_path = event.file_path, r.event_time = event.event_time)",
    "tasks.max": "3"
  }
}